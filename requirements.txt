# CHROMADB COMPATIBLE - Works with Python 3.11+
# Remote embeddings mode ONLY - NO local PyTorch/ONNX models

# Core LangGraph and LangChain
langgraph>=0.0.55
langchain>=1.0.3
langchain-core>=1.0.0
langchain-community>=0.4.1
langchain-groq>=1.0.0
langchain-chroma>=1.0.0

# Remote embeddings via HuggingFace Inference API (NO local models)
langchain-huggingface>=0.1.0
huggingface-hub>=0.20.0

# LLM and API clients
groq>=0.4.2

# Vector Database (acts as storage only, NO built-in embeddings)
chromadb>=0.4.22

# Essential utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
typing-extensions>=4.8.0

# Web Framework
flask>=3.0.0

# Production WSGI Server
gunicorn>=21.2.0

# Data Processing
numpy>=1.24.0

# Web Scraping (for knowledge base updates)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0
schedule>=1.2.0

# Multi-Format File Support (Optional - for enhanced knowledge base)
# These are used by agent/update_agent.py for parsing different file types
PyPDF2>=3.0.0
python-docx>=1.0.0
pandas>=2.0.0

# Re-ranker Support - DISABLED by default to prevent local model downloads
# DO NOT UNCOMMENT unless you have sufficient RAM (2GB+) and set RERANKER_ENABLED=true
# Uncommenting these will trigger ONNX/torch downloads and break Render free tier deployment
# sentence-transformers>=2.2.0
# torch>=2.0.0