# CHROMADB COMPATIBLE - Works with Python 3.11+
# Remote embeddings mode (default) - no local PyTorch required

# Core LangGraph and LangChain
langgraph>=0.0.55
langchain>=1.0.3
langchain-core>=1.0.0
langchain-community>=0.4.1
langchain-groq>=1.0.0
langchain-chroma>=1.0.0
langchain-huggingface>=1.0.1

# LLM and API clients
groq>=0.4.2

# Vector Database with compatible embeddings
chromadb>=0.4.22

# Essential utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
typing-extensions>=4.8.0

# Web Framework
flask>=3.0.0

# Production WSGI Server
gunicorn>=21.2.0

# Data Processing
numpy>=1.24.0

# Web Scraping (for knowledge base updates)
beautifulsoup4>=4.12.0
requests>=2.31.0
lxml>=4.9.0
schedule>=1.2.0

# Multi-Format File Support (Optional - for enhanced knowledge base)
# These are used by agent/update_agent.py for parsing different file types
PyPDF2>=3.0.0
python-docx>=1.0.0
pandas>=2.0.0

# Re-ranker Support (OPTIONAL - Disabled by default)
# Uncomment these lines if you want to enable local re-ranker (RERANKER_ENABLED=true)
# Requires Python 3.11 and significant memory (~2GB)
# sentence-transformers>=2.2.0
# torch>=2.0.0